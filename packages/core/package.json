{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "@aurajs/core",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build:watch": "tsc -p tsconfig.json --watch",
    "typedoc": "typedoc src --out docs --tsconfig tsconfig.json"
  },
  "dependencies": {
    "eventemitter3": "^5.0.1",
    "tslib": "^2.6.2",
    "ws": "^8.16.0"
  },
  "devDependencies": {
    "@types/ws": "^8.5.10"
  }
}
